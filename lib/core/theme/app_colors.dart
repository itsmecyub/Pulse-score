import 'package:flutter/material.dart';

/// PulseScore palette — a deep navy ground with a single electric-green accent.
class AppColors {
  AppColors._();

  // Grounds
  static const bg = Color(0xFF0B1220);
  static const bgElevated = Color(0xFF101A2B);
  static const surface = Color(0xFF131D2E);
  static const surfaceAlt = Color(0xFF162133);

  // Lines
  static const border = Color(0xFF1E2A3D);
  static const borderStrong = Color(0xFF2A3852);

  // Accent
  static const green = Color(0xFF2BE06B);
  static const greenBright = Color(0xFF3DEE7C);
  static const greenDeep = Color(0xFF0E3A21);
  static const greenGlow = Color(0x332BE06B);

  // Live / alert
  static const live = Color(0xFFFF4D4F);
  static const liveDeep = Color(0xFF3A1418);
  static const amber = Color(0xFFFFB020);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8A94A6);
  static const textTertiary = Color(0xFF5C6678);
  static const textFaint = Color(0xFF3E4759);
}
